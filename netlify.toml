[build]
  command = "npm run build"
  publish = "dist"
  functions = "lambda"

[context.staging]
  command = "npm run stage"

[context.donato_datocms_client_library_update]
  command = "npm run stage"
